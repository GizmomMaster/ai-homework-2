{
  "name": "telegram-local-llm-bot",
  "version": "1.0.0",
  "description": "Telegram-бот, пересылающий сообщения в локальную LLM (Ollama) без сторонних Telegram-библиотек",
  "type": "module",
  "private": true,
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test test/"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0"
  }
}
